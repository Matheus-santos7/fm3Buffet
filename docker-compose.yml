version: '3.8'

services:
  app:
    image: theussm/fm3buffet:1.0.8
    ports:
      - "3000:80"
    environment:
      - DATABASE_HOST=database-1.cr0y0caswj27.us-east-2.rds.amazonaws.com
      - DATABASE_USER=root
      - DATABASE_PASSWORD=1101Matheus
      - DATABASE_NAME=deliveryOnline
    depends_on:
      - db
